specVersion: "v1"
serviceId: "weather-track-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "weather-track-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
